import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Vercel deployment - dynamic server rendering enabled
  // Images optimized automatically by Vercel
};

export default nextConfig;
